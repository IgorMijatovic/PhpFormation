<div class="col-sm-4">
    <div class="card">
        <div class="card-block">
            <h4 class="card-title">{{ count }}</h4>
            <p class="card-text">Article{% if count > 1 %}{% endif %}</p>
            <a href="{{ path('blog.admin.index')}}" class="btn btn-danger">Gerer les articles</a>
        </div>
    </div>
</div>