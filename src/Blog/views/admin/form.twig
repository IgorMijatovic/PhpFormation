{{ field('name', item.name, "Titre de l'artile") }}
{{ field('slug', item.slug, "Lien") }}
{{ field('content', item.content, "Content", {type: 'textarea'}) }}
{{ field('created_at', item.created_at, "Date de creation", {class: 'datepicker'}) }}